# Runtime API dependencies
fastapi
uvicorn
python-multipart
aiofiles
python-dotenv

# ML inference dependencies
numpy
librosa

# Force CPU-only PyTorch on Linux (Render) to avoid huge CUDA downloads.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.11.0+cpu; platform_system == "Linux"
torch==2.11.0; platform_system != "Linux"